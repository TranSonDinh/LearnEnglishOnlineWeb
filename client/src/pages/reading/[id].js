import React, { memo } from "react";
import PropTypes from "prop-types";

const ReadingDetail = (props) => {
  return <div>ReadingDetail</div>;
};

ReadingDetail.propTypes = {};

export default memo(ReadingDetail);
