import React, { memo } from "react";
import PropTypes from "prop-types";

const ListeningDetail = (props) => {
  return <div>ListeningDetail</div>;
};

ListeningDetail.propTypes = {};

export default memo(ListeningDetail);
