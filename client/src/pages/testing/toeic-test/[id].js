import React, { memo } from "react";
import PropTypes from "prop-types";

const TestingDetail = (props) => {
  return <div>TestingDetail</div>;
};

TestingDetail.propTypes = {};

export default memo(TestingDetail);
